<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="16dp"
            android:paddingTop="16dp"
            android:paddingBottom="8dp">

            <ImageButton
                android:id="@+id/btn_voltar_fatura_list"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_arrow_back"
                android:tint="?attr/colorOnSurface"
                android:contentDescription="@string/voltar" />

            <TextView
                android:id="@+id/tv_titulo_fatura_list"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="@string/faturas_titulo"
                android:textColor="?attr/colorOnSurface"
                android:textSize="18sp"
                android:textStyle="bold" />

        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_ir_compras_cartao"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="8dp"
            android:insetTop="10dp"
            android:insetBottom="10dp"
            android:icon="@drawable/ic_chevron_right"
            app:iconGravity="textEnd"
            app:iconPadding="12dp"
            android:text="@string/faturas_ir_para_compras"
            android:textAllCaps="false" />

        <TextView
            android:id="@+id/tv_faturas_vazio"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/faturas_vazio"
            android:gravity="center"
            android:paddingVertical="32dp"
            android:visibility="gone"
            android:textColor="?attr/colorOnSurfaceVariant" />

        <!-- ViewPager2 horizontal para navegar entre faturas -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/vp_faturas"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>

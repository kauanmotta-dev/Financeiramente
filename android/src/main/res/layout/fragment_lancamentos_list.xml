<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cinza_background">

    <!-- AppBar contendo a SearchBar M3 -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_lancamentos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/cinza_background"
        app:elevation="0dp">

        <com.google.android.material.search.SearchBar
            android:id="@+id/search_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginVertical="8dp"
            android:hint="@string/buscar_lancamentos"
            app:navigationIcon="@null"
            app:defaultQueryHint="@string/buscar_lancamentos" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- RecyclerView com comportamento de scroll abaixo do AppBar -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_lancamentos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- Empty state — Épico 8 -->
    <include
        android:id="@+id/layout_empty_state"
        layout="@layout/layout_empty_state"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:visibility="gone" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_novo_lancamento"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@android:drawable/ic_input_add"
        android:contentDescription="@string/add_lancamento" />

    <!-- SearchView M3 — overlay por cima de tudo -->
    <com.google.android.material.search.SearchView
        android:id="@+id/search_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="@string/buscar_lancamentos" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
